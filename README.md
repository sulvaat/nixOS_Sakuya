# nixOS_Sakuya

NixOS configuration for **Sakuya**, my `x86_64-linux` desktop. Flake-based, with
[Home Manager](https://github.com/nix-community/home-manager) for the user
environment and [Stylix](https://github.com/nix-community/stylix) for system-wide
theming.

## Highlights

| | |
|---|---|
| **Compositor** | [Niri](https://github.com/YaLTeR/niri) (Wayland, scrollable-tiling) |
| **Bar / launcher / notifications** | Waybar · Fuzzel · Mako |
| **Wallpaper** | swww (via an `awww` compatibility shim) + Waypaper |
| **Theme** | Stylix — Tokyo City Dark, JetBrainsMono Nerd Font |
| **Shell** | Fish + Starship |
| **GPU** | AMD (`amdgpu`), 32-bit support for games |
| **Audio** | PipeWire (PulseAudio compat) |
| **Virtualization** | libvirtd + virt-manager |
| **Gaming / apps** | Steam, Flatpak, AppImage |

Triple-monitor layout: a 32:9 5120×1440 (DP-1), plus two 2560×1440 panels
(DP-2 rotated 90°, HDMI-A-1).

## Layout

The config is split into small, focused modules. The two entrypoints just wire
imports and a few globals.

```
.
├── flake.nix                  # inputs: nixpkgs (unstable), home-manager, stylix
├── configuration.nix          # system entrypoint (imports + Home Manager, nix, locale, state)
├── home.nix                   # Home Manager entrypoint (imports + user identity)
├── hardware-configuration.nix # generated by nixos-generate-config
├── pano.jpg                   # wallpaper referenced by Stylix
└── modules/
    ├── system/                # NixOS modules
    │   ├── boot.nix           #   bootloader, kernel, amdgpu
    │   ├── hardware.nix       #   graphics, OpenRazer, controllers
    │   ├── networking.nix     #   firewall, Avahi/mDNS, NetworkManager, SSH
    │   ├── audio.nix          #   PipeWire
    │   ├── desktop.nix        #   Xorg, Niri enable, portals, picom, fonts
    │   ├── stylix.nix         #   theme, wallpaper, fonts
    │   ├── virtualisation.nix #   libvirtd, virt-manager
    │   ├── filesystems.nix    #   NFS + btrfs mounts
    │   ├── programs.nix       #   Steam, AppImage, Flatpak, Fish enable
    │   ├── packages.nix       #   environment.systemPackages
    │   └── users.nix          #   user account
    └── home/                  # Home Manager modules
        ├── packages.nix       #   per-user packages
        ├── niri.nix           #   Niri config + swww startup helpers
        ├── waybar.nix         #   Waybar config + window-list helper
        ├── services.nix       #   Flameshot, Mako, swayidle, KDE Connect
        ├── theming.nix        #   GTK theme/icons/cursor
        ├── shells.nix         #   Fish, Bash, Starship
        └── programs.nix       #   Fuzzel, Kitty, Ghostty, Git, Yazi, Zoxide
```

To add or change something, edit the relevant module — Nix merges them all, so
list options like `environment.systemPackages` and `home.packages` can be
declared wherever they fit best.

## Usage

```sh
# Rebuild and switch to this configuration
cd /etc/nixos && sudo nixos-rebuild switch --flake .

# Rebuild and update flake inputs (nixpkgs, home-manager, stylix)
cd /etc/nixos && sudo nixos-rebuild switch --flake . --upgrade
```

Handy shell aliases defined in the config:

| Alias | Action |
|-------|--------|
| `rb`  | `cd /etc/nixos && sudo nixos-rebuild switch --flake .` |
| `rbu` | same, with `--upgrade` |
| `vc`  | edit `configuration.nix` |
| `vh`  | edit `home.nix` |
| `ns`  | `nix search nixpkgs` |

> A `warning: Git tree '/etc/nixos' is dirty` during a rebuild just means there
> are uncommitted changes — it's harmless. Commit when the build looks good.

## Notes

- Secrets and editor/Nix backups are kept out of the repo via `.gitignore`.
- `pano.jpg` (the wallpaper) is committed because Stylix references it directly.
- First version installed: NixOS `25.05` (`system.stateVersion`).
